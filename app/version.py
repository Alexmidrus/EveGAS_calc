"""Единственное место, где записан номер версии.

Отдельным модулем, а не строкой в ``app/__init__.py``, по одной причине:
сюда обязан ходить и конфиг, и сборщик цен, а тянуть ради номера версии
фабрику приложения вместе с Flask незачем.

**Правило версионирования.** Номер поднимается только при появлении нового
функционала. Исправления, рефакторинг и правки документации версию не двигают:
иначе номер перестаёт что-либо означать и превращается в счётчик коммитов.
"""

__version__ = "0.2.0"

__all__ = ["__version__"]
