# Локальный конфиг с личным контактом в User-Agent — не в репозиторий.
# Слэш обязателен: без него правило накрывало и app/config.py — источник
# умолчаний, без которого приложение не поднимается из свежего клона.
/config.py

# Рабочие данные: файл SQLite dev-профиля, блокировки, локальные логи
var/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Окружения
.venv/
venv/
env/
.env

# Тесты и линтеры
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Инструменты разработки: настройки агентов и редакторов, не часть проекта
.claude/
.vscode/
.idea/
*.swp
*~

# Мусор файловых менеджеров
.DS_Store
Thumbs.db

# Ресурсы для разработки. SDE — это 107 МБ входных данных для
# tools/build_gases.py; в репозитории лежит только результат — data/gases.json
sde.zip
sde/
*.zip

# Макет «GasLens Terminal» от 19.08.2026 — справочный материал этапа 12, а не
# часть приложения: 600 КБ прототипа с React-рантаймом внутри. Вёрстка и палитра
# из него переезжают в app/static/style.css, правила — в docs/SPEC.md §10.
/EveGAS_calc дизайн/
